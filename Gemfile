# Gemfile für Jekyll mit Forty-Theme und Mermaid-Unterstützung

# Standard-Gems für Jekyll
source "https://rubygems.org"

# GitHub Pages-Compliance (wichtig für die korrekte Build-Umgebung auf GitHub)
gem "github-pages", group: :jekyll_plugins

# Forty Jekyll Theme (wird remote geladen, siehe _config.yml)
gem "jekyll-remote-theme"

# Mermaid-Unterstützung für Diagramme
gem "jekyll-mermaid"

# Optional: Für lokale Entwicklung (falls du Jekyll lokal testen willst)
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
